---

- hosts: "{{ servers }}"
  gather_facts: false
  serial: 1
  tasks:
    - name: Sleep for 5 minutes and continue with play
      wait_for:
        timeout: 300
      delegate_to: localhost 
      

       
